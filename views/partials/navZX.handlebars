<nav class="navbar navbar-expand-md navbar-dark bg-dark mb-4">
    <a class="navbar-brand" href="{{rootAddress}}">For
        <strong>documentation</strong> purposes only</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse"
        aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{rootAddress}}">Home
                    <span class="sr-only">(current)</span>
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{rootAddress}}/airports">Airports home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{rootAddress}}/airports/airportList">Airport Listing</a>
            </li>
            {{#if atc}}
            <a href="{{rootAddress}}/{{airport.icao}}/pilot">
                <button class="btn btn-outline-success">Pilot view</button>
            </a> {{else if pilot}}
            <a href="{{rootAddress}}/{{airport.icao}}/atc">
                <button class="btn btn-outline-success">ATC view</button>
            </a> {{/if}}
        </ul>
        <form class="form-inline mt-2 mt-md-0">
            <input class="form-control mr-sm-2 disabled" type="text" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form>

    </div>
</nav>